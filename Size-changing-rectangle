int a=0;
int b=0;
void setup (){
  size(800,600);
}
void draw(){
  background(0,0,0);
  if(mousePressed==true){
    background(231,80,0);
    a=mouseX;
    b=mouseY;
  }
      rect(a,b,mouseX-a,mouseY-b);
}
